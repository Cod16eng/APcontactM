<div class=" col-md-9" ><%= simple_form_for([@contact, @note]) do |f| %>
  <div><%= f.input :content, label: "Edit note", as: :text, :input_html => { :rows => 10 } %></div>
  <%= f.button :submit, class: "btn btn-success" %>
<% end %>
</div>

