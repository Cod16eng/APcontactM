<div class="col-md-9">
 <%= simple_form_for([@contact, @contact.notes.build]) do |f| %>
   <%= f.input :content, label: "New note", as: :text, :input_html => { :rows => 10 } %>
   <%= f.button :submit, class: "btn btn-success" %>
 <% end %>
</div>